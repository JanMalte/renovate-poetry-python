[tool.poetry]
name = "renovate-poetry-python"

[tool.poetry.dependencies]
python = ">=3.13.2,<3.14"
django = "==4.2.6"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
